

const Card = ({text}) => {
    //Handle click function?

    return (
        // onClick event?
        <button className='Card'>
            <p className={text}></p>
        </button>
    );
}

export default Card();
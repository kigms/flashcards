import React from 'react';
import './Card.css'; // TODO: See if in right spot

const Card = () => {
    const question = "";
    const answer = "";
    
    //Handle click function?

    return (
        // onClick event?
        <div className="card">
            <p>Question</p>
        </div>
    );
}

export default Card;